@page "/"

<h1>BackedBy Playground</h1>
<p>Canonical sample of CretNet's <code>BackedBy&lt;TQuery&gt;</code> data source. The grid below
   pages, searches and sorts entirely against an in-memory list — no API, no database — but
   uses the exact same wiring pattern WAM uses against EF + Postgres.</p>

<SampleCustomersGrid />
